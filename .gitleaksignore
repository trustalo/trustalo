# Fingerprints listed here are silenced by the Gitleaks CI scan.
#
# The CI scan runs in working-tree mode (`gitleaks detect --no-git`),
# so fingerprints DO NOT include a commit SHA. Format:
#
#   <path>:<rule-id>:<line>
#
# Only add entries that are confirmed false positives -- values that LOOK
# like secrets but are not (deterministic dev fallbacks, fixture data,
# hash test vectors, etc.). Real secrets must be rotated, not ignored.
#
# When adding an entry, leave a short comment above it explaining WHY the
# match is safe so reviewers don't have to re-derive that on every audit.
